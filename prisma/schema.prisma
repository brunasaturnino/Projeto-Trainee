generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:../database/database.db"
}


model Usuarios {
  id Int @id @default(autoincrement())
  email String @unique
  nome String 
  senha String
  foto String? // ? represnta opcional
  privilegio Boolean
}